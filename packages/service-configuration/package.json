{
  "name": "@gx-mob/service-configure",
  "version": "0.0.1",
  "description": "Plugin to load gx services configurations",
  "main": "./dist/index.js",
  "files": [
    "dist",
    "types"
  ],
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/GX-mob/http-service-template.git"
  },
  "scripts": {
    "test": "NODE_ENV=test jest --runInBand",
    "lint": "eslint '*/**/*.{js,ts,tsx}' --quiet --fix",
    "build": "rimraf ./dist && tsc --sourceMap -p ./"
  },
  "author": "ferco0",
  "license": "AGPL-3.0",
  "dependencies": {
    "fastify": "^3.1.1",
    "fastify-plugin": "^2.0.3"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.6.0",
    "eslint-config-prettier": "^6.11.0",
    "eslint-plugin-prettier": "^3.1.4",
    "jest": "^26.4.0",
    "jest-runner-groups": "^2.0.1",
    "mongodb-memory-server": "^6.6.4",
    "stream-buffers": "^3.0.2",
    "ts-jest": "^26.2.0"
  }
}
