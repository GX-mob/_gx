import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "column",
    height: "100%",
    alignItems: "center",
  },
  row: {
    width: "80%",
    height: "40%",
    alignItems: "center",
    flexDirection: "column",
  },
});
