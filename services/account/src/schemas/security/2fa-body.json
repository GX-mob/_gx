{
  "title": "Update 2FA Body Schema",
  "type": "object",
  "properties": {
    "password": { "type": "string", "pattern": "^(?=.*\\d)(?=.*[a-z]).{4,}$" },
    "2fa": { "type": "string", "pattern": "^(?=.*\\d)(?=.*[a-z]).{4,}$" }
  },
  "additionalProperties": false,
  "required": ["password", "2fa"]
}
