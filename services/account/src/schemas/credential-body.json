{
  "title": "Update Credential Body Schema",
  "type": "object",
  "properties": {
    "current": { "type": "string", "pattern": "^(?=.*\\d)(?=.*[a-z]).{4,}$" },
    "new": { "type": "string", "pattern": "^(?=.*\\d)(?=.*[a-z]).{4,}$" }
  },
  "additionalProperties": false,
  "required": ["current", "new"]
}
