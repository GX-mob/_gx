{
  "title": "Credentials Body Schema",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "credential": { "type": "string", "pattern": "^(?=.*\\d)(?=.*[a-z]).{5,}$" }
  },
  "additionalProperties": false,
  "required": ["id", "credential"]
}
