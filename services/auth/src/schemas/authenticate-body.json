{
  "title": "Authenticate Body Schema",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "credential": { "type": "string" }
  },
  "additionalProperties": false,
  "required": ["id", "credential"]
}
